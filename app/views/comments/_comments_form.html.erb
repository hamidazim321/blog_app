<%= form_with(model: [:user, @post, @comment], data: { turbo: false }, local: true) do |f|%>
  <%= f.text_area :text, placeholder: 'comment...', class: 'form-control mb-2'  %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>