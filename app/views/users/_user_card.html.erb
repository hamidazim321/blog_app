<div class="d-flex gap-4">
  <div class="border p-2 w-auto"> Image placeholder</div>
  <div class="border p-2 w-100 d-flex justify-content-between align-items-center">
    <%= link_to user.name, user_path(user), class: "fs-1 text-decoration-none text-dark" %> 
    <p class="align-self-end">Number of posts: <span><%= user.posts_counter %></span></p>
  </div>
</div>